server:
  port: 80
  session-timeout: 30
  tomcat:
    max-threads: 0
    uri-encoding: utf-8
spring:
  thymeleaf:
    encoding: utf-8
    servlet:
      content-type: text/html
    cache: false
  datasource:
    url: jdbc:mysql://192.168.2.171:3306/red_myb?useUnicode=true&characterEncoding=utf-8&autoReconnect=true&failOverReadOnly=false&useSSL=false&useTimezone=true&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Admin123!
  jpa:
    open-in-view: false
logback:
  # 日志根文件
  file: E:/usr/logs
mybatis:
  mapper-locations: classpath*:mapper/*.xml,classpath*:auto/mapper/*.xml